package relake.settings.implement;

import relake.settings.Setting;

public class DelimiterSetting extends Setting<String> {
    public DelimiterSetting(String name) {
        super(name);
    }
}